# flowspect needs no secrets to run. This file exists so contributors know
# where env-based configuration would live; copy to .env (untracked) if needed.
# Example (only used by optional tooling, never required):
# GITHUB_TOKEN=
